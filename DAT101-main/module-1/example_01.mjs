"use strict"
import { printOut } from "../../common/script/utils.mjs";

const person =
{
    name: "Arne",
    age: 30,
    city: "Oslo"
};

printOut(person.name);
printOut(person.age);
printOut(person.city); 